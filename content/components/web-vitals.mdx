---
title: Web Vitals
description: next/web-vitals를 사용하여 웹 성능 지표(Web Vitals)를 측정하고 Google Analytics로 전송하는 컴포넌트입니다.
---


## 설치

<Tabs items={["Command", "Manual"]}>
  <Tab value="Command">
    **pnpm**

```bash
pnpm dlx shadcn@latest add @chaesunbak/web-vitals
```

**npm**

```bash
npx shadcn@latest add @chaesunbak/web-vitals
```
  </Tab>
  <Tab value="Manual">
   <div className="fd-steps">
    <div className="fd-step" >
      <p>다음 코드를 프로젝트에 복사/붙혀넣기 하세요.</p>
        <ComponentSource src="registry/default/components/web-vitals.tsx" title="components/web-vitals.tsx" />
    </div>
    <div className="fd-step" >
      <p>관련 의존성을 추가하세요.</p>  
   </div>
   </div>
  </Tab>
</Tabs>

## 사용법

```tsx
import { GoogleAnalytics } from '@next/third-parties/google'
import { WebVitals } from "@/components/web-vitals";
```

```tsx
<>
  <GoogleAnalytics gaId="G-XXXXXXXXXX" />
  <WebVitals />
</>
```

## 인터페이스

### WebVitals

이 컴포넌트는 별도의 Props를 받지 않습니다.
